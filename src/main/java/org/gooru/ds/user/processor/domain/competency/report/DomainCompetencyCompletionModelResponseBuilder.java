
package org.gooru.ds.user.processor.domain.competency.report;

import java.util.ArrayList;
import java.util.List;
import java.util.Map;
import org.gooru.ds.user.constants.Constants;

/**
 * @author szgooru Created On 30-Jan-2019
 */
public class DomainCompetencyCompletionModelResponseBuilder {

  /*public static DomainCompetencyCompletionModelResponse buildReponse(String agent,
      Map<String, DomainCompetencyCompletionModel> domainCompetencyCompletionMap, int memberCount) {
    if (agent.equalsIgnoreCase(Constants.Params.AGENT_MOBILE)) {
      return buildResponseForMobile(domainCompetencyCompletionMap, memberCount);
    } else {
      return buildResponseForDesktop(domainCompetencyCompletionMap, memberCount);
    }
  }

  private static DomainCompetencyCompletionModelResponse buildResponseForDesktop(

  }

  private static DomainCompetencyCompletionModelResponse buildResponseForMobile(

  }*/
}
